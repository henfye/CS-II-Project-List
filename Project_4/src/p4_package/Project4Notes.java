package p4_package;

public class Project4Notes
   {
      // Do not make more constants within the program
      // Can use blocks of code for a single comment, dont need for every line
      // initialize the conio outside the methods so it is global
      // if( !isLeapYear()) is the same as is not leap year
   }
